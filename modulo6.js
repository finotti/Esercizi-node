var fs = require('fs');
var path = require('path');

module.exports = function es6 (perc, ext, callback) {
function callback (err,data){
   if (err) throw err;
   for (i=0; i<=list.length; i++){
	b=path.extname(list[i]);
	if (b == a){
	   console.log(list[i]);
	}
   }
}

}
